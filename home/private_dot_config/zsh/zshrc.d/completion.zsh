# -----------------------------------------------------
# Completion system — powered by zsh-autocomplete (in 00-zinit.zsh)
# -----------------------------------------------------

# Use cached completion for fast startup (zsh-autocomplete handles the rest)
autoload -Uz compinit
compinit -C
