#############################################################################
# Secrets — shared with zsh via ~/.config/shell/secrets.sh (see BASH_UPGRADE).
#############################################################################
source "$HOME/.config/shell/secrets.sh"
